// function sum() {
//     var nums = Array.prototype.slice.call(arguments, 0);

//     return nums.reduce(function (base, curr) {
//         return base + curr;
//     });
// }

// function sum(...nums) {
//     return nums.reduce((base, curr) => base + curr);
// }

// var data = [100, 4, 600, 40];

// var max = Math.max.apply(null, data);
// var max = Math.max(...data);

// console.log(max);
